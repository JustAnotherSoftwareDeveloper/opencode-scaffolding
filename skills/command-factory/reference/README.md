# Command Factory Reference

Keep writes bounded to one `commands/<name>.md` file.
Require supported frontmatter, explicit workflow and constraints, resolvable skill
and agent references, and passing Markdown lint.
