# Reference Files

Index of reference files for `skill-script-python-writer`.

- `./cli-conventions.md` — Click argument vs option conventions, JSON stdout default, exit code meanings, error message formatting, and the complete `count-tokens` example.
- `./python-style-guide.md` — Ruff lint rules, pyright type-check settings, import ordering, naming conventions, type annotation style, and coverage exemptions.
- `./path-conventions.md` — Global and project-local script root resolution order, directory layout, and pyproject.toml hatchling packages configuration.
- `./shared-lib-rules.md` — Five rules for shared library modules, import conventions, and file layout under `src/lib/shared/`.