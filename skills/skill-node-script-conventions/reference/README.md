# Node Script Conventions — Reference Index

- **`typescript-node-style-guide.md`** — TypeScript and Node.js coding style conventions including Biome lint rules, strict-mode flags, import ordering, naming conventions, type annotation patterns, and coverage exemptions.
- **`shared-lib-rules.md`** — Five rules for shared library modules under `src/lib/shared/`: no CLI entry points, 100% coverage, consumer documentation, domain-based naming, and extraction discipline.
- **`path-layout.md`** — Directory and file path conventions for Node scripts, including `src/` layout, test layout, and import conventions.
- **`tooling-config.md`** — Tooling configuration conventions for `biome.json`, `tsconfig.json`, and `package.json` with recommended settings and dependency lists.
- **`bun-test-conventions.md`** — Conventions for writing and organizing `bun:test` test files under `tests/`, covering `Bun.spawnSync`, fixture setup, parameterized tests, mocking, and file mapping.
- **`coverage-strategy.md`** — Coverage targets, reporting, threshold policy, edge case checklist, error path testing requirements, and exemption conventions.
- **`test-examples.md`** — Complete runnable test examples for CLI integration tests, file-based unit tests, module mocking, spy patterns, parameterized tests, and shared fixture helpers.
