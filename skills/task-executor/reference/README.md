# Task Execution Contract

- Accept one canonical task object with optional `verification`.
- Load task-declared skills directly as inline context.
- Enforce declared skill, read, write, execution, verification, and output boundaries.
- Deny worker delegation through the `task` tool.
