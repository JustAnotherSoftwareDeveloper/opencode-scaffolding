# Reference Files

Index of reference files for `skill-script-node-writer`.

- `./cli-conventions.md` — Cleye CLI patterns, exit code conventions, error message formatting, and the complete `lint-md` example.
- `./path-layout.md` — Directory layout, import conventions, shared lib pattern, and Node vs Python platform selection guidance.
- `./tooling-config.md` — biome.json configuration, tsconfig.json options, package.json script entries, dependency conventions, and resolution order.