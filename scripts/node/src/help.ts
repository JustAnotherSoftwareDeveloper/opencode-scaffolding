const lines: string[] = [
  'Available scripts:',
  '  help     Show this help message',
  '  run      Run the placeholder Node entry point from src/',
  '  example  Run the example Node script from src/'
]

for (const line of lines) {
  console.log(line)
}
